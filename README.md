This article is both a tutorial and a reference for the Linux `printf` command. It begins by introducing the purpose and syntax of `printf` and gradually increases the knowledge of the reader until they can copy and customize command patterns.

> [!IMPORTANT]
> The files for this vault are maintained locally in an Obsidian vault, which uses a different linking syntax than GitHub-flavored Markdown (GFM). The table of contents still serves as a guide to what's in the article, but the links do not work properly on GitHub. However,  if there are any links in the article that point to outside of this repo, those links should work.

Without further ado, learn [how to use `printf`](printf.md).
